import React from "react";

import {CURRENT_ORDER_ACTIVE} from "utils";

import {FlexibleTable} from "components";

const Active = () => {
    return <FlexibleTable header={CURRENT_ORDER_ACTIVE} body={[]}/>
}

export default Active