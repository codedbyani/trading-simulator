import React from "react";

import {CURRENT_ORDER_MMR_CLOSE} from "utils";

import {FlexibleTable} from "components";

const MMRClose: React.FC = () => {
    return <FlexibleTable header={CURRENT_ORDER_MMR_CLOSE} body={[]}/>
}

export default MMRClose