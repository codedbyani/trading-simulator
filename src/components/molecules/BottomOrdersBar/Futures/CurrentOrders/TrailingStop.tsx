import React from "react";

import {CURRENT_ORDER_TRAILING_STOP} from "utils";

import {FlexibleTable} from "components";

const TrailingStop: React.FC = () => {
    return <FlexibleTable header={CURRENT_ORDER_TRAILING_STOP} body={[]}/>
}

export default TrailingStop