import React from "react";

import {CURRENT_ORDER_CONDITIONAL} from "utils";

import {FlexibleTable} from "components";

const Conditional: React.FC = () => {
    return <FlexibleTable header={CURRENT_ORDER_CONDITIONAL} body={[]}/>
}

export default Conditional