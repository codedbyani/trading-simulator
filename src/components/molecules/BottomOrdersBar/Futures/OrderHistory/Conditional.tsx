import React from "react";

import {ORDER_HISTORY_CONDITIONAL} from "utils";

import {FlexibleTable} from "components";

const Conditional: React.FC = () => {
    return <FlexibleTable header={ORDER_HISTORY_CONDITIONAL} body={[]}/>
}

export default Conditional