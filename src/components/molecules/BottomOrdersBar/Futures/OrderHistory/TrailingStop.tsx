import React from "react";

import {ORDER_HISTORY_TRAILING_STOP} from "utils";

import {FlexibleTable} from "components";

const TrailingStop: React.FC = () => {
    return <FlexibleTable header={ORDER_HISTORY_TRAILING_STOP} body={[]}/>
}

export default TrailingStop