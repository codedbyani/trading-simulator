import React from "react";

import {ORDER_HISTORY_MMR_CLOSE} from "utils";

import {FlexibleTable} from "components";

const MMRClose: React.FC = () => {
    return <FlexibleTable header={ORDER_HISTORY_MMR_CLOSE} body={[]}/>
}

export default MMRClose